# Contracts Directory

This directory holds canonical interface contracts used by both client and server layers.

Suggested contents:

- API request/response schemas
- domain event schemas
- validation contracts shared across modules

Rule: behavioral contract changes must be versioned and reviewed with corresponding docs/tests updates.
